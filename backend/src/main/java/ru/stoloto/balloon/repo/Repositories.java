package ru.stoloto.balloon.repo;

/**
 * Пакет доступа к данным.
 *
 * <p>Каждый репозиторий — узкий интерфейс Spring Data над одной сущностью.
 * Запросы, которые вызываются на каждом кадре полёта (живой рейтинг), здесь
 * отсутствуют намеренно: такие данные держатся в памяти и сбрасываются в БД
 * по завершении раунда, см. {@code TournamentService}.
 */
final class Repositories {
    private Repositories() {
    }
}
